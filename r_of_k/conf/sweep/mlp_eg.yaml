# @package _global_
defaults:
  - _self_
  #- n_sweep
  #- override hydra/launcher: submitit_slurm
hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model: mlp
      seed: 1, 2, 3
      opt.update_algorithm: eg
      opt.lr: 0.1, 0.25, 0.5, 0.75, 1
      opt.wd: 0

