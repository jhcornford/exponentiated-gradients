# @package _global_
# python train.py --multirun launcher=one_gpu +sweep=mlp_eg
defaults:
  - override /hydra/launcher: submitit_slurm

hydra:
  launcher:
    # timeout_min: 60
    # cpus_per_task: null
    # gpus_per_node: null
    # tasks_per_node: 1
    mem_gb: 8
    array_parallelism: 256
    # nodes: 1
    # name: ${hydra.job.name}
    # #_target_: hydra_plugins.hydra_submitit_launcher.submitit_launcher.SlurmLauncher
    # partition: null
    # qos: null
    # comment: null
    # constraint: null
    # exclude: null
    gres: gpu:1
    # cpus_per_gpu: null
    # gpus_per_task: null
    # mem_per_gpu: null
    # mem_per_cpu: null
    # account: null
    # signal_delay_s: 120
    # max_num_timeout: 0
    # additional_parameters: {}
    # setup: null
    # srun_args: null
